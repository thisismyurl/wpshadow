# Help Cards

Each subdirectory in this folder defines a single help card for the Help page.

- One card per directory.
- Each card defines a `card.php` that returns a configuration array.
- Cards are loaded and rendered by the Help page module.
